# راهنمای نصب خودکار پخشش کن!

## 📋 پیش‌نیازها

- سیستم عامل: Ubuntu 20.04+ یا Debian 11+
- دسترسی root یا sudo
- اتصال به اینترنت
- حداقل 1GB RAM و 10GB فضای دیسک

## 🚀 نصب سریع

### برای سرور ایران (Panel):

```bash
sudo bash install.sh
```

اسکریپت به صورت خودکار:
- ✅ نوع سرور را تشخیص می‌دهد (ایران یا خارجی)
- ✅ دامنه را تشخیص می‌دهد یا از IP استفاده می‌کند
- ✅ تمام پکیج‌های لازم را نصب می‌کند
- ✅ دیتابیس را ایجاد و پیکربندی می‌کند
- ✅ V2Ray را نصب و پیکربندی می‌کند
- ✅ SSL را تنظیم می‌کند (در صورت وجود دامنه)
- ✅ فایل‌های پروژه را کپی می‌کند
- ✅ Composer dependencies را نصب می‌کند
- ✅ Apache را پیکربندی می‌کند
- ✅ Firewall را تنظیم می‌کند
- ✅ Admin user را ایجاد می‌کند

### برای سرور خارجی (V2Ray Node):

```bash
sudo bash install.sh
```

اسکریپت به صورت خودکار:
- ✅ نوع سرور را تشخیص می‌دهد
- ✅ V2Ray را نصب می‌کند
- ✅ کد یکتای سرور را تولید می‌کند
- ✅ Monitoring service را راه‌اندازی می‌کند
- ✅ Network optimization را اعمال می‌کند

## 📝 اطلاعات دسترسی

پس از نصب، تمام اطلاعات مهم در فایل زیر ذخیره می‌شود:

```
/var/log/pakhsheshkon_install_summary.txt
```

این فایل شامل:
- URL پنل
- نام کاربری و رمز عبور Admin
- اطلاعات دیتابیس
- پورت V2Ray
- کد یکتای سرور (برای سرورهای خارجی)

⚠️ **مهم**: این فایل را در جای امن ذخیره کنید!

## 🔧 تشخیص خودکار

### تشخیص نوع سرور:

اسکریپت به صورت خودکار با بررسی IP سرور، نوع آن را تشخیص می‌دهد:
- **ایران**: IP های ایرانی (5.x.x.x, 37.x.x.x, 46.x.x.x, ...)
- **خارجی**: سایر IP ها

### تشخیص دامنه:

اسکریپت به ترتیب:
1. Reverse DNS lookup
2. Hostname
3. IP Address (در صورت عدم وجود دامنه)

## 📦 ساختار نصب

```
/var/www/html/                    # مسیر نصب (پیش‌فرض)
├── src/                          # کدهای PHP
│   ├── Core/                     # کلاس‌های اصلی
│   ├── Models/                   # مدل‌های دیتابیس
│   ├── Controllers/              # کنترلرها
│   ├── Services/                 # سرویس‌ها
│   └── Helpers/                  # Helper ها
├── public/                       # فایل‌های عمومی
│   ├── index.php                 # Entry point
│   ├── assets/                   # CSS, JS, Fonts
│   └── qrcodes/                  # QR Code ها
├── views/                        # View ها
├── database/                     # Migration ها
├── storage/                      # Logs, Backups, Cache
├── .env                          # تنظیمات
└── composer.json                 # Dependencies
```

## 🔐 امنیت

- تمام رمزهای عبور به صورت تصادفی تولید می‌شوند
- SSL/TLS به صورت خودکار تنظیم می‌شود
- Firewall به صورت خودکار پیکربندی می‌شود
- Security headers در Apache تنظیم می‌شوند
- فایل `.env` با دسترسی 600 محافظت می‌شود

## 🛠️ عیب‌یابی

### لاگ‌ها:

```bash
# لاگ نصب
tail -f /var/log/pakhsheshkon.log

# خلاصه نصب
cat /var/log/pakhsheshkon_install_summary.txt

# لاگ Apache
tail -f /var/log/apache2/pakhsheshkon-error.log

# لاگ V2Ray
tail -f /var/log/v2ray/error.log
```

### مشکلات رایج:

1. **خطای Composer**: 
   ```bash
   composer self-update
   composer install --no-interaction
   ```

2. **خطای دیتابیس**:
   ```bash
   systemctl status mariadb
   mysql -u root -p
   ```

3. **خطای Apache**:
   ```bash
   systemctl status apache2
   apache2ctl configtest
   ```

4. **خطای V2Ray**:
   ```bash
   systemctl status v2ray
   v2ray test -config /usr/local/etc/v2ray/config.json
   ```

## 🔄 به‌روزرسانی

برای به‌روزرسانی:

```bash
cd /var/www/html
git pull origin main
composer install --no-interaction --optimize-autoloader
php database/migrations/run.php
```

## 🗑️ حذف نصب

برای حذف کامل:

```bash
sudo bash install.sh
# گزینه 2 را انتخاب کنید
```

یا به صورت دستی:

```bash
systemctl stop apache2 mariadb v2ray pakhsheshkon-monitor
systemctl disable apache2 mariadb v2ray pakhsheshkon-monitor
rm -rf /var/www/html/* /etc/pakhsheshkon /usr/local/etc/v2ray
mysql -e "DROP DATABASE IF EXISTS pk_*;"
```

## 📞 پشتیبانی

برای مشکلات و سوالات:
- Issues: [GitHub Issues](https://github.com/mahdikbk/pakhshesh-kon/issues)
- Documentation: [README.md](README.md)

## ✅ چک‌لیست پس از نصب

- [ ] پنل در دسترس است
- [ ] می‌توانید با Admin login کنید
- [ ] V2Ray در حال اجرا است
- [ ] SSL فعال است (در صورت وجود دامنه)
- [ ] Firewall تنظیم شده است
- [ ] Backup خودکار فعال است
- [ ] Monitoring service در حال اجرا است (سرورهای خارجی)

---

**نکته**: این اسکریپت به صورت کاملاً خودکار طراحی شده و نیاز به ورودی کاربر ندارد. تمام تنظیمات به صورت خودکار انجام می‌شود.

