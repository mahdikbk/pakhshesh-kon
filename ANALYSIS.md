# تحلیل کامل پروژه پخشش کن! (Pakhshesh Kon)

## 📋 خلاصه اجرایی

**پخشش کن!** یک پنل مدیریت VPN پیشرفته است که با استفاده از **V2Ray** برای مدیریت سرورهای ایران و خارجی طراحی شده است. این پروژه یک سیستم کامل و یکپارچه برای مدیریت کاربران، سرورها، مانیتورینگ و تولید لینک‌های اتصال VPN ارائه می‌دهد.

---

## 🏗️ معماری پروژه

### ساختار کلی
پروژه به صورت یک **اسکریپت نصب Bash** طراحی شده که تمام فایل‌های PHP، CSS، JS و تنظیمات را در حین نصب تولید می‌کند. این رویکرد مزایا و معایبی دارد:

**مزایا:**
- نصب ساده و یکپارچه
- تولید خودکار تنظیمات بر اساس محیط
- عدم نیاز به مدیریت دستی فایل‌های متعدد

**معایب:**
- کدهای PHP در داخل اسکریپت Bash (سخت‌تر برای نگهداری)
- عدم امکان توسعه مستقل فایل‌ها
- مشکل در debugging

### معماری سیستم

```
┌─────────────────────────────────────────┐
│         سرور ایران (Panel)               │
│  ┌──────────────────────────────────┐    │
│  │  Apache + PHP + MariaDB         │    │
│  │  - Dashboard                    │    │
│  │  - User Management              │    │
│  │  - Server Management            │    │
│  │  - Monitoring                   │    │
│  └──────────────────────────────────┘    │
│  ┌──────────────────────────────────┐    │
│  │  V2Ray (VLESS+TLS)              │    │
│  │  Port: Random (10000-60000)    │    │
│  └──────────────────────────────────┘    │
└─────────────────────────────────────────┘
              ↕ HTTP/HTTPS
┌─────────────────────────────────────────┐
│      سرورهای خارجی (Abroad)             │
│  ┌──────────────────────────────────┐  │
│  │  V2Ray (VLESS+TLS, VMess+WS)    │  │
│  │  - Monitor Script               │  │
│  │  - Auto Registration            │  │
│  └──────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

---

## 🛠️ تکنولوژی‌های استفاده شده

### Backend
- **PHP**: زبان اصلی برای منطق برنامه
- **MariaDB/MySQL**: پایگاه داده
- **Apache**: وب سرور
- **V2Ray**: پروتکل VPN

### Frontend
- **Tailwind CSS**: فریمورک CSS (از CDN)
- **Chart.js**: برای نمودارها
- **JavaScript**: منطق سمت کلاینت
- **فونت یکان**: فونت فارسی

### ابزارها و کتابخانه‌ها
- **Composer**: مدیریت وابستگی‌های PHP
- **Endroid QR Code**: تولید QR کد
- **Certbot**: مدیریت SSL/TLS
- **vnstat**: مانیتورینگ ترافیک
- **jq**: پردازش JSON

---

## 📊 ساختار دیتابیس

### جداول اصلی

#### 1. `admins`
```sql
- id (INT, PRIMARY KEY)
- username (VARCHAR(50))
- password (VARCHAR(255)) -- با bcrypt hash شده
```

#### 2. `users`
```sql
- id (INT, PRIMARY KEY)
- username (VARCHAR(50))
- uuid (VARCHAR(36)) -- UUID برای V2Ray
- server_group_id (INT) -- گروه سرور
- traffic_limit (BIGINT) -- محدودیت ترافیک (bytes)
- traffic_used (BIGINT) -- ترافیک مصرف شده
- connection_limit (INT) -- تعداد اتصال همزمان
- expiry_date (DATE) -- تاریخ انقضا
- qr_path (VARCHAR(255)) -- مسیر QR کد
- link (TEXT) -- لینک V2Ray
- created_at (TIMESTAMP)
```

#### 3. `server_groups`
```sql
- id (INT, PRIMARY KEY)
- name (VARCHAR(50)) -- نام گروه (مثل "اروپا")
- created_at (TIMESTAMP)
```

#### 4. `servers`
```sql
- id (INT, PRIMARY KEY)
- group_id (INT) -- گروه سرور
- ip (VARCHAR(15))
- port (INT)
- name (VARCHAR(50))
- unique_code (VARCHAR(36)) -- کد یکتا برای شناسایی
- status (ENUM: 'active', 'inactive')
- created_at (TIMESTAMP)
```

#### 5. `monitoring`
```sql
- id (INT, PRIMARY KEY)
- server_id (INT)
- active_users (INT)
- bandwidth (VARCHAR(50))
- ping (INT)
- recorded_at (TIMESTAMP)
```

#### 6. `tickets`
```sql
- id (INT, PRIMARY KEY)
- title (VARCHAR(100))
- message (TEXT)
- user_id (INT)
- created_at (TIMESTAMP)
```

#### 7. `logs`
```sql
- id (INT, PRIMARY KEY)
- action (VARCHAR(255))
- username (VARCHAR(50))
- ip (VARCHAR(45))
- created_at (TIMESTAMP)
```

---

## 🔐 امنیت

### نقاط قوت
1. **رمزنگاری رمز عبور**: استفاده از `password_hash()` با bcrypt
2. **SSL/TLS**: پشتیبانی از HTTPS با Let's Encrypt
3. **Session Management**: استفاده از PHP sessions
4. **SQL Injection Protection**: استفاده از prepared statements
5. **Firewall**: پیکربندی UFW
6. **SSH Hardening**: تغییر پورت SSH و غیرفعال کردن root login (در سرورهای خارجی)

### نقاط ضعف و خطرات
1. **عدم استفاده از CSRF Token**: فرم‌ها در برابر حملات CSRF آسیب‌پذیرند
2. **عدم Rate Limiting**: امکان brute force روی صفحه لاگین
3. **عدم Input Validation**: برخی ورودی‌ها به درستی validate نمی‌شوند
4. **Hardcoded Paths**: مسیرهای ثابت در کد
5. **عدم استفاده از HTTPS در monitor.php**: داده‌های مانیتورینگ به صورت HTTP ارسال می‌شوند
6. **عدم رمزنگاری unique_code**: کد سرور به صورت plain text ذخیره می‌شود
7. **عدم استفاده از API Key**: monitor.php بدون احراز هویت قابل دسترسی است

---

## ⚙️ ویژگی‌های اصلی

### 1. مدیریت کاربران
- ایجاد کاربر با UUID یکتا
- تولید خودکار لینک V2Ray
- تولید QR کد برای کاربران
- محدودیت ترافیک و تعداد اتصال
- تاریخ انقضا
- دانلود گروهی لینک‌ها (ZIP)

### 2. مدیریت سرورها
- ثبت سرورهای خارجی با کد یکتا
- گروه‌بندی سرورها
- ثبت خودکار سرورهای خارجی
- تست پینگ

### 3. مانیتورینگ
- نمایش real-time پینگ، ترافیک و کاربران فعال
- نمودارهای Chart.js
- ارسال خودکار داده‌ها از سرورهای خارجی (هر 5 دقیقه)
- نمایش آمار در داشبورد

### 4. رابط کاربری
- طراحی مدرن با Tailwind CSS
- فونت یکان برای فارسی
- انیمیشن‌های CSS
- پشتیبانی از تم روشن/تاریک (localStorage)
- Responsive Design

### 5. سیستم تیکت
- ایجاد تیکت پشتیبانی
- مدیریت تیکت‌ها

### 6. لاگ‌ها
- ثبت تمام فعالیت‌ها
- نمایش لاگ‌ها در پنل

### 7. به‌روزرسانی
- آپدیت خودکار از GitHub با AJAX

---

## 🔄 فرآیند نصب

### سرور ایران
1. اجرای اسکریپت نصب
2. انتخاب نوع سرور (ایران)
3. وارد کردن اطلاعات:
   - نام کاربری و رمز ادمین
   - دامنه
   - مسیر URL (اختیاری)
4. نصب خودکار:
   - به‌روزرسانی سیستم
   - نصب Apache, PHP, MariaDB
   - نصب V2Ray
   - تولید SSL certificate
   - ایجاد دیتابیس و جداول
   - تولید فایل‌های PHP
   - پیکربندی Apache
   - تنظیم Firewall

### سرور خارجی
1. اجرای اسکریپت نصب
2. انتخاب نوع سرور (خارجی)
3. وارد کردن:
   - نام سرور
   - دامنه پنل ایران
   - مسیر URL پنل
4. نصب خودکار:
   - نصب V2Ray
   - تولید کد یکتا (UUID)
   - پیکربندی V2Ray
   - بهینه‌سازی شبکه (BBR)
   - امن‌سازی SSH
   - راه‌اندازی سرویس مانیتورینگ
   - ثبت خودکار در پنل ایران

---

## 📁 ساختار فایل‌ها

### فایل‌های اصلی
```
install.sh              # اسکریپت نصب اصلی
monitor.sh              # اسکریپت مانیتورینگ سرورهای خارجی
README.md               # مستندات
LICENSE                 # لایسنس GNU GPL v3
logo-PK.png            # لوگو پروژه
```

### فایل‌های تولید شده در نصب (سرور ایران)

#### PHP Files
- `index.php` - صفحه ورود
- `dashboard.php` - داشبورد با نمودارها
- `users.php` - مدیریت کاربران
- `servers.php` - مدیریت سرورها
- `server-groups.php` - مدیریت گروه‌های سرور
- `monitoring.php` - مانیتورینگ سرورها
- `settings.php` - تنظیمات
- `update.php` - به‌روزرسانی پنل
- `tickets.php` - سیستم تیکت
- `logs.php` - نمایش لاگ‌ها
- `ping.php` - API تست پینگ
- `monitor.php` - دریافت داده‌های مانیتورینگ

#### Includes
- `includes/auth.php` - احراز هویت
- `includes/db.php` - اتصال دیتابیس
- `includes/functions.php` - توابع اصلی
- `includes/server-key.php` - مدیریت کدهای سرور
- `includes/config.php` - تنظیمات
- `includes/nav.php` - منوی ناوبری

#### Assets
- `assets/css/style.css` - استایل‌های سفارشی
- `assets/js/script.js` - اسکریپت‌های JavaScript
- `assets/fonts/Yekan.ttf` - فونت یکان

#### Directories
- `qrcodes/` - ذخیره QR کدها

---

## 🔍 تحلیل کد

### نقاط قوت کد

1. **سازماندهی خوب**: کدها به صورت منطقی تقسیم شده‌اند
2. **استفاده از PDO**: محافظت در برابر SQL Injection
3. **Prepared Statements**: در تمام query ها استفاده شده
4. **Error Handling**: استفاده از try-catch در اتصال دیتابیس
5. **Logging**: ثبت لاگ برای تمام فعالیت‌ها
6. **Load Balancing**: الگوریتم انتخاب بهترین سرور بر اساس پینگ و بار

### نقاط ضعف کد

1. **عدم استفاده از Namespace**: تمام کدها در global scope
2. **عدم استفاده از Class**: کدها procedural هستند
3. **Hardcoded Values**: مقادیر ثابت در کد
4. **عدم استفاده از Environment Variables**: تنظیمات در config.php hardcode شده
5. **عدم استفاده از Dependency Injection**: وابستگی‌ها مستقیم هستند
6. **عدم استفاده از MVC Pattern**: کدها به صورت مستقیم HTML تولید می‌کنند
7. **عدم استفاده از Template Engine**: HTML در PHP embed شده
8. **عدم استفاده از API Versioning**: API ها version ندارند
9. **عدم استفاده از Caching**: داده‌ها هر بار از دیتابیس خوانده می‌شوند
10. **عدم استفاده از Queue**: عملیات‌های سنگین به صورت synchronous انجام می‌شوند

### مشکلات امنیتی در کد

1. **عدم CSRF Protection**:
```php
// در users.php و servers.php
<form method="POST"> // بدون CSRF token
```

2. **عدم Rate Limiting**:
```php
// در auth.php
function login($username, $password) {
    // بدون محدودیت تعداد تلاش
}
```

3. **عدم Input Validation**:
```php
// در users.php
$username = $_POST['username']; // بدون sanitize
```

4. **عدم استفاده از HTTPS در monitor.php**:
```bash
# در monitor.sh
curl -X POST http://... # باید https باشد
```

5. **عدم احراز هویت در monitor.php**:
```php
// در monitor.php
if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    // بدون بررسی API key یا token
}
```

---

## 🚀 عملکرد و بهینه‌سازی

### نقاط قوت
1. **استفاده از BBR**: در سرورهای خارجی برای بهینه‌سازی شبکه
2. **Apache Optimization**: تنظیمات KeepAlive و compression
3. **Load Balancing**: توزیع کاربران بین سرورها

### نقاط ضعف
1. **عدم استفاده از Redis/Memcached**: برای cache
2. **عدم استفاده از CDN**: برای فایل‌های استاتیک
3. **عدم استفاده از Database Indexing**: برای جداول بزرگ
4. **عدم استفاده از Query Optimization**: برخی query ها بهینه نیستند
5. **عدم استفاده از Lazy Loading**: تمام داده‌ها یکجا لود می‌شوند

---

## 📈 مقیاس‌پذیری

### محدودیت‌ها
1. **Single Database**: عدم پشتیبانی از replication
2. **Single Server**: عدم پشتیبانی از load balancing در سطح application
3. **Synchronous Operations**: عملیات‌های سنگین blocking هستند
4. **عدم استفاده از Message Queue**: برای عملیات async

### پیشنهادات
1. استفاده از Redis برای session storage
2. استفاده از Message Queue (RabbitMQ, Redis Queue)
3. استفاده از Database Replication
4. استفاده از Load Balancer (Nginx, HAProxy)
5. استفاده از Microservices Architecture

---

## 🧪 تست‌ها

### وضعیت فعلی
- **هیچ تستی وجود ندارد**
- عدم استفاده از Unit Testing
- عدم استفاده از Integration Testing
- عدم استفاده از E2E Testing

### پیشنهادات
1. استفاده از PHPUnit برای Unit Tests
2. استفاده از Selenium برای E2E Tests
3. استفاده از Postman برای API Tests
4. استفاده از Code Coverage Tools

---

## 📚 مستندات

### وضعیت فعلی
- README.md با توضیحات خوب
- کامنت‌های محدود در کد
- عدم وجود API Documentation
- عدم وجود Architecture Documentation

### پیشنهادات
1. استفاده از PHPDoc برای مستندسازی توابع
2. ایجاد API Documentation (Swagger/OpenAPI)
3. ایجاد Architecture Diagrams
4. ایجاد User Guide
5. ایجاد Developer Guide

---

## 🔄 نگهداری و توسعه

### مشکلات
1. **کدهای PHP در Bash Script**: سخت برای نگهداری
2. **عدم استفاده از Version Control برای فایل‌های تولید شده**
3. **عدم استفاده از CI/CD**
4. **عدم استفاده از Code Review Process**

### پیشنهادات
1. **جداسازی کدها**: استخراج PHP files از install.sh
2. **استفاده از Template Engine**: برای تولید فایل‌ها
3. **استفاده از CI/CD**: GitHub Actions, GitLab CI
4. **استفاده از Docker**: برای محیط توسعه
5. **استفاده از Composer**: برای مدیریت وابستگی‌ها

---

## 🎯 پیشنهادات بهبود

### اولویت بالا (امنیت)
1. ✅ اضافه کردن CSRF Protection
2. ✅ اضافه کردن Rate Limiting
3. ✅ اضافه کردن Input Validation و Sanitization
4. ✅ استفاده از HTTPS در monitor.php
5. ✅ اضافه کردن API Key برای monitor.php
6. ✅ رمزنگاری unique_code

### اولویت متوسط (عملکرد)
1. ✅ اضافه کردن Redis برای Cache
2. ✅ بهینه‌سازی Query ها
3. ✅ اضافه کردن Database Indexing
4. ✅ استفاده از CDN برای فایل‌های استاتیک
5. ✅ اضافه کردن Lazy Loading

### اولویت پایین (توسعه)
1. ✅ Refactoring به MVC Pattern
2. ✅ استفاده از Namespace و Class
3. ✅ اضافه کردن Unit Tests
4. ✅ استفاده از Docker
5. ✅ اضافه کردن CI/CD

---

## 📊 خلاصه ارزیابی

### امتیاز کلی: 7/10

| معیار | امتیاز | توضیحات |
|------|--------|---------|
| عملکرد | 8/10 | عملکرد خوب با BBR و بهینه‌سازی‌ها |
| امنیت | 5/10 | نیاز به بهبود در CSRF، Rate Limiting و Input Validation |
| مقیاس‌پذیری | 6/10 | محدودیت در مقیاس‌پذیری |
| نگهداری | 5/10 | کدهای PHP در Bash script سخت برای نگهداری |
| مستندات | 7/10 | README خوب اما نیاز به مستندات بیشتر |
| تست | 0/10 | هیچ تستی وجود ندارد |

---

## 🎓 نتیجه‌گیری

**پخشش کن!** یک پروژه کامل و کاربردی برای مدیریت VPN است که ویژگی‌های خوبی دارد اما نیاز به بهبود در زمینه‌های امنیت، تست و نگهداری دارد. با اعمال پیشنهادات ارائه شده، می‌تواند به یک سیستم حرفه‌ای و قابل اعتماد تبدیل شود.

### نقاط قوت اصلی:
- ✅ نصب ساده و خودکار
- ✅ رابط کاربری زیبا
- ✅ ویژگی‌های کامل
- ✅ پشتیبانی از V2Ray
- ✅ مانیتورینگ real-time

### نقاط ضعف اصلی:
- ❌ مشکلات امنیتی
- ❌ عدم وجود تست
- ❌ کدهای PHP در Bash script
- ❌ عدم مقیاس‌پذیری

---

**تاریخ تحلیل**: $(date)
**نسخه پروژه**: 1.0.0
**تحلیل‌گر**: AI Assistant

